<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Biite.Pages.ProfilePage"
             Title="Profile">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">


            <Frame BackgroundColor="GhostWhite" Padding="20" CornerRadius="12">
                <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="15">

                    <!--added media picker for picture taking-->
                    <Frame Grid.RowSpan="2" BackgroundColor="#E4A44D" CornerRadius="30" HeightRequest="60" WidthRequest="60" Padding="0" HasShadow="False">
                        <Grid>
                            <!-- shows image if exists, otherwise show letter -->
                            <Image x:Name="ProfileImage" Source="{Binding ProfileImagePath}" Aspect="AspectFill" IsVisible="{Binding HasProfileImage}">
                                <Image.Clip>
                                    <EllipseGeometry Center="30,30" RadiusX="30" RadiusY="30"/>
                                </Image.Clip>
                            </Image>
                            <!-- shows letter P if no image-->
                            <Label x:Name="DefaultProfileLabel" Text="P" FontSize="20" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="True"/>
                        </Grid>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnProfileImageTapped"/>
                        </Frame.GestureRecognizers>
                    </Frame>

                    
                    <Label Grid.Column="1" Text="Your Name" FontAttributes="Bold" FontSize="18"/>
                    <Label Grid.Column="1" Grid.Row="1" Text="just a girl" FontSize="14" TextColor="Gray"/>

                     <!-- added the number of reviews from sql instead of just having a text in place like before-->
                    <Label Grid.Column="1" Grid.Row="2" Text="{Binding ReviewCountText}" FontSize="12" TextColor="#E4A44D" Margin="0,10,0,0"/>


                    <HorizontalStackLayout Grid.Column="1" Grid.Row="3" Spacing="10" Margin="0,15,0,0">
                        <Button Text="Edit Profile" BackgroundColor="#E4A44D" TextColor="Black" CornerRadius="8" Clicked="OnEditProfileClicked"/>
                        <Button Text="View Reviews" BackgroundColor="#E4A44D" TextColor="Black" CornerRadius="8" Clicked="OnViewReviewsClicked"/>
                    </HorizontalStackLayout>
                </Grid>
            </Frame>
            <!-- preferneces -->
            <Frame BackgroundColor="GhostWhite" Padding="20" CornerRadius="12">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Your Dietary Preferences" FontAttributes="Bold" FontSize="16"/>
                    <Frame BackgroundColor="#E4A44D" CornerRadius="15" Padding="8,5" HorizontalOptions="Start">
                        <Label Text="Vegetarian" TextColor="White" FontSize="12"/>
                    </Frame>
                </VerticalStackLayout>
            </Frame>

            <!-- recent act -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid ColumnDefinitions="*,Auto">
                    <Label Text="My Recent Reviews" FontAttributes="Bold" FontSize="18"/>

                </Grid>
                <Frame Grid.Row="1" Padding="12" CornerRadius="12" BackgroundColor="GhostWhite" Opacity="0.9">
                    <VerticalStackLayout>
                        <Label Text="{Binding LastReview.RestaurantName}" FontAttributes="Bold"/>
                        <Label Text="{Binding LastReview.ReviewSnippet}"/>
                        <Label Text="{Binding LastReview.Stars, StringFormat='{0} stars'}"/>
                    </VerticalStackLayout>
                </Frame>
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>